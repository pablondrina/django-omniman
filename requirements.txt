django>=5.0,<6.0
djangorestframework>=3.15,<4.0
# Admin UI (Core do framework: integração pronta via Unfold)
django-unfold>=0.49,<1.0
# Dynamic settings via admin (não para dados sensíveis!)
django-constance>=4.0,<5.0
django-picklefield>=3.0,<4.0
# Audit history for models
django-simple-history>=3.7,<4.0
# Image handling
Pillow>=10.0,<11.0
# Django Cotton - Component-based templates
django-cotton>=2.6.0,<3.0

